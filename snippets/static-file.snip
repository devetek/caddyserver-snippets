# snippets/static-file
# {args.0} represents the root url of the app. Example: "terpusat.com".
# {args.1} represents the folder location. Example: "/var/www/public"

(static-file) {
    {args.0} {
        root * {args.1}
        file_server

        log {
            output file /var/log/caddy/{args.0}.access.log {
                roll_size 100mb
                roll_keep 3
                roll_keep_for 720h
            }
        }
    }
}